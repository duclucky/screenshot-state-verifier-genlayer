# AGENTS.md

This is a standalone GenLayer Intelligent Contract submission.

Do not create frontend, dashboard, Vercel app, npm project, Next.js app, or Tailwind setup.

Use Python unittest only:

```bash
python -m unittest discover -s tests -p "test_*.py" -v
```

Do not deploy or run GenLayer CLI unless explicitly requested.
